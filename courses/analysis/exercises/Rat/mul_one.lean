import Rat.one_mul

namespace Analysis
open MyRat

/- `x * 1 = x`, from earlier laws. Neither this nor `add_zero` needs
representatives. -/
theorem MyRat.mul_one (x : MyRat) : x * 1 = x := by
  sorry

end Analysis
