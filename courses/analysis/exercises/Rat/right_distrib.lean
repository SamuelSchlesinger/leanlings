import Rat.left_distrib

namespace Analysis
open MyRat

/- Right distributivity `(x + y) * z = x * z + y * z`. Like `left_distrib`. -/
theorem MyRat.right_distrib (x y z : MyRat) : (x + y) * z = x * z + y * z := by
  sorry

end Analysis
