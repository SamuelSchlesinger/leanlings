import Rat.mul_zero

namespace Analysis
open MyRat

/- Left distributivity `x * (y + z) = x * y + x * z`. -/
theorem MyRat.left_distrib (x y z : MyRat) : x * (y + z) = x * y + x * z := by
  sorry

end Analysis
