import Rat.neg_neg

namespace Analysis
open MyRat

/- Commutativity of multiplication — the multiplicative analogue of `add_comm`,
with `mul_mk` instead of `add_mk`. -/
theorem MyRat.mul_comm (x y : MyRat) : x * y = y * x := by
  sorry

end Analysis
