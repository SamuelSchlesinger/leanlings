import Rat.zero_add

namespace Analysis
open MyRat

/- `x + 0 = x`. This follows from two laws you have already proved, without
returning to representatives. Explain why two earlier laws are enough. -/
theorem MyRat.add_zero (x : MyRat) : x + 0 = x := by
  sorry

end Analysis
