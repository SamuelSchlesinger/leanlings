import Cauchy.neg_isCauchy

namespace Analysis
open MyRat

/- `CauchyEquiv` (difference tends to zero) is an equivalence relation; the
next three levels prove it. Reflexivity. -/
theorem equiv_refl (f : ℕ → MyRat) : CauchyEquiv f f := by
  sorry

end Analysis
