import Real.zero_add

namespace Analysis
open MyReal

/- `x + 0 = x`. You have already transported the laws this needs from
rational representatives to reals; use them without reopening the
quotient. -/
theorem MyReal.add_zero (x : MyReal) : x + 0 = x := by
  sorry

end Analysis
