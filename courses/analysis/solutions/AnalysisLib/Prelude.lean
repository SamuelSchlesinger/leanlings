/-
Everything an `analysis` level needs in scope to begin: the rational numbers.
The first exercise imports this; later exercises import the previous level (and
so, transitively, this), plus the infrastructure modules that introduce Cauchy
sequences, the reals, and metric spaces further along the course.
-/
import AnalysisLib.Rat
