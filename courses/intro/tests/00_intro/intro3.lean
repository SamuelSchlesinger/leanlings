-- Hidden correctness checks for this exercise (not shown to the learner).
#guard isReady == true
#guard count > 0
#guard message == "hello"
#check (isReady : Bool)
#check (count : Nat)
#check (message : String)
